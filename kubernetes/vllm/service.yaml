apiVersion: v1
kind: Service
metadata:
  name: vllm-qwen
  namespace: vllm
  labels:
    app: vllm-qwen
spec:
  type: ClusterIP
  selector:
    app: vllm-qwen
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
