# IRSA for S3 model read (model-seed job + vLLM init container). Apply before model-seed.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vllm
  namespace: vllm
  annotations:
    eks.amazonaws.com/role-arn: __VLLM_MODEL_S3_ROLE_ARN__
